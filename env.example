# Firebase Configuration
# Get these values from Firebase Console → Project Settings → General → Your apps → Web app
FIREBASE_API_KEY=
FIREBASE_AUTH_DOMAIN=
FIREBASE_PROJECT_ID=
FIREBASE_STORAGE_BUCKET=
FIREBASE_MESSAGING_SENDER_ID=
FIREBASE_APP_ID=

# Google OAuth Configuration
# Get these values from Google Cloud Console → APIs & Services → Credentials → OAuth 2.0 Client IDs
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Admin Configuration
# Email address that will have admin privileges (can delete rides)
ADMIN_EMAIL=
